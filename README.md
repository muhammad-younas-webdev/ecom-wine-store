
  # E-commerce Wine Store

  This is a code bundle for E-commerce Wine Store. The original project is available at https://www.figma.com/design/Ta3jlaShQ7CAfs2U8OCWo1/E-commerce-Wine-Store.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  